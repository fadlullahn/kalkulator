<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/nila"
    tools:context=".MainActivity">

    <!-- Judul Aplikasi -->
    <TextView
        android:id="@+id/textViewTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Kalkulator Pakan Ikan"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@android:color/white"
        android:gravity="center"
        android:layout_marginTop="32dp"
        android:layout_marginBottom="16dp"
        android:shadowColor="@android:color/black"
        android:shadowDx="10"
        android:shadowDy="10"
        android:shadowRadius="8"/>


    <!-- Deskripsi Aplikasi -->
    <TextView
        android:id="@+id/textViewDescription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Untuk Budidaya Ikan"
        android:textSize="18sp"
        android:textColor="@android:color/white"
        android:textAlignment="center"
        android:layout_below="@id/textViewTitle"
        android:layout_marginBottom="32dp"
        android:shadowColor="@android:color/black"
        android:shadowDx="15"
        android:shadowDy="15"
        android:shadowRadius="12"/>

    <!-- Tombol FR -->
    <Button
        android:id="@+id/btnFR"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="FR"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/btnSR"
        android:layout_marginTop="16dp"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="32dp"/>

    <!-- Tombol SR -->
    <Button
        android:id="@+id/btnSR"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="SR"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/textViewDescription"
        android:layout_marginTop="16dp"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="32dp"/>

    <!-- Tombol SGR -->
    <Button
        android:id="@+id/btnSGR"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="SGR"
        android:layout_below="@id/btnFR"
        android:layout_marginTop="16dp"
        android:layout_marginStart="32dp"
        android:layout_centerHorizontal="true"
        android:layout_marginEnd="32dp"/>

    <!-- Tombol FCR -->
    <Button
        android:id="@+id/btnFCR"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="FCR"
        android:layout_below="@id/btnSGR"
        android:layout_marginTop="24dp"
        android:layout_centerHorizontal="true"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="32dp"/>

    <!-- Tombol FCR -->
    <Button
        android:id="@+id/btnChart"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Chart"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/btnFCR"
        android:layout_marginTop="24dp"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="32dp"/>

</RelativeLayout>
